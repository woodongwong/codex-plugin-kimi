---
description: Retrieve a compact Kimi Code handoff and Git checkpoint
argument-hint: [job-id]
---

Call `kimi_result` for `$ARGUMENTS` with `full=false`. Inspect the returned Git diff and tests independently. Request full output only if the structured handoff is insufficient to diagnose a failure.
