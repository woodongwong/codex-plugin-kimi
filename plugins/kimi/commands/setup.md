---
description: Check whether Kimi Code is ready for delegated work
---

Call `kimi_setup`. Report whether the Kimi CLI is installed. If authentication fails only when a task starts, tell the user to run `kimi login` and retry.
